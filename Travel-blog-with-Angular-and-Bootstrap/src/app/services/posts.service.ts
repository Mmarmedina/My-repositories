import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})

export class PostsService {

  // Definir, inicializar un array (recibe BBDD un array con datos entidad posts)

  constructor() { }

  // Método getAllPost (): donde se recuperan todos los post. 

  // newPost(post): añadir nuevo post al array de objetos Posts. 

  // getPostsByCategoria(cat)


  
}
